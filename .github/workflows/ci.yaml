name: CI Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  PLATFORM: linux/amd64
  EVENT_NUMBER: ${{ github.event.number || github.sha }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Container Registry
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.TOKEN }}

      - name: Build and Push Container Image
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: hello-world:${{ env.EVENT_NUMBER }}

      - name: kubectl
        uses: actions-hub/kubectl@master
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        with:
          args: set image deployment/hello-world hello-world=hello-world:${{ env.EVENT_NUMBER }} 

      #- name: Envsubst K8s Manifest
      #  if: github.event_name == 'pull_request'
      #  env:
      #    PLUGIN_PULL_REQUEST_NUM: ${{ github.event.number }}
      #  run: |
      #    cat /etc/os-release
      #    sudo apt update && sudo apt-get install -y gettext-base
      #    envsubst < manifest-beta.yaml > manifest-beta-build.yaml
      #    cat manifest-beta-build.yaml

      #- name: Apply to K8s 
      #  uses: hongkunyoo/kubectl-actions@main
      #  if: github.event_name == 'pull_request'
      #  env:
      #    KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG }}
      #  with:
      #    args: kubectl apply -n ops -f manifest-beta-build.yaml

      #- name: Create GitHub Comment
      #  if: github.event_name == 'pull_request'
      #  uses: peter-evans/create-or-update-comment@v4
      #  with:
      #    issue-number: ${{ github.event.number }}
      #    body: |
      #      Thank you for your contribution! 👋 <br> Your draft page is
      #            [https://docs-beta-${{env.EVENT_NUMBER}}-example.com](https://docs-beta-${{env.EVENT_NUMBER}}-example.com).
      #    reactions: '+1'

